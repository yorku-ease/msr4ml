rule "check if data from name"
when
    get_categorie_by_name(name, categorie="data") = True
then
    assign(categorie="data", priority=0)
end

rule "check if model from name"
when
    get_categorie_by_name(name, categorie="model") = True
then
    assign(categorie="model", priority=0)
end

rule "check if configuration from name"
when
    get_categorie_by_name(name, categorie="conf") = True
then
    assign(categorie="conf", priority=0)
end

rule "check if model from extension"
when
    get_categorie_by_extension(name, extension['model']) = True
then
    assign(categorie="model", priority=1)
end

rule "check if conf from extension"
when
    get_categorie_by_extension(name, extension['conf']) = True
then
    assign(categorie="conf", priority=1)
end

rule "check if data from extension"
when
    get_categorie_by_extension(name, extension['data']) = True
then
    assign(categorie="data", priority=1)
end